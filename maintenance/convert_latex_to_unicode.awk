#!/usr/bin/awk -f

{
    gsub(/\\'\{a\}|{\\'a}/, "á")
    gsub(/\\'\{e\}|{\\'e}/, "é")
    gsub(/\\'\{i\}|{\\'i}/, "í")
    gsub(/\\'\{o\}|{\\'o}/, "ó")
    gsub(/\\'\{u\}|{\\'u}/, "ú")
    gsub(/\\\^\{a\}|{\\\^a}/, "â")
    gsub(/\\\^\{e\}|{\\\^e}/, "ê")
    gsub(/\\\^\{o\}|{\\\^o}/, "ô")
    gsub(/\\`\{a\}|{\\`a}/, "à")
    gsub(/\\`\{e\}|{\\`e}/, "è")
    gsub(/\\`\{i\}|{\\`i}/, "ì")
    gsub(/\\`\{o\}|{\\`o}/, "ò")
    gsub(/\\`\{u\}|{\\`u}/, "ù")
    gsub(/\\~\{a\}|{\\~a}/, "ã")
    gsub(/\\~\{o\}|{\\~o}/, "õ")
    gsub(/\\"\{a\}|{\\"a}/, "ä")
    gsub(/\\"\{e\}|{\\"e}/, "ë")
    gsub(/\\"\{i\}|{\\"i}/, "ï")
    gsub(/\\"\{o\}|{\\"o}/, "ö")
    gsub(/\\"\{u\}|{\\"u}/, "ü")
}
1

